<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head') %> 
    <body>
        <%- include('../partials/header') %> 
       <div>
        <ul>
        <div> <%users.forEach(({name, city})=>{%>
            <li><%= name %> is from <%= city %> </li>
        <%}) %>
        </div>
        </ul>
        
    </div>
    </body>
</html>